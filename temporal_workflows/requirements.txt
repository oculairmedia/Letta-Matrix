# Temporal worker dependencies for matrix-file-queue
# Pin same versions as Letta's temporal worker for compatibility

temporalio==1.5.0
httpx==0.27.0
websockets>=13.0
asyncpg>=0.29.0

# MarkItDown for document parsing (same as matrix-client)
markitdown[all]>=0.1.0

# Document parser dependencies (OCR fallback)
PyMuPDF>=1.24.0
pytesseract>=0.3.10
Pillow>=10.0.0

# Transitive dependency: src/ is volume-mounted and src.core.__init__
# eagerly imports modules that depend on aiohttp and nio
aiohttp>=3.9.0
python-olm>=3.2.16
matrix-nio>=0.24.0
